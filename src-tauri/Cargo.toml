[package]
name = "seo-lens-desktop"
version = "0.1.0"
edition = "2021"
authors = ["SEO Lens Team"]
description = "Native cross-platform desktop shell for SEO Lens"
license = "MIT OR Apache-2.0"

[package.metadata.dist]
dist = false

[dependencies]
seo-lens = { path = ".." }
